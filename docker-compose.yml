services:
  web:
    image: customer-api-v1  # Using Flask Application Image
    ports:
      - "5000:5000"  # Port forwarding for Flask API
    environment:
      - DATABASE_URL  # Postgres Connection Url
      - BASE_URL      # from .env file
    depends_on:
      - db  # The container with Postgres starts earlier
    restart:
      always

  db:
    image: postgres  # Use the official Postgres Image for prod DB
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Gl00m88
      POSTGRES_DB: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart:
      always

#  test_db:
#    image: postgres  # Separate service for the test DB
#    environment:
#      POSTGRES_USER: test_user
#      POSTGRES_PASSWORD: test_password
#      POSTGRES_DB: test_db
#    volumes:
#      - test_postgres_data:/var/lib/postgresql/data
#    ports:
#      - "5433:5432"  # Running on a different port
#    restart:
#      always

volumes:
  postgres_data:
#  test_postgres_data:  # Volume for storing the test DB data
